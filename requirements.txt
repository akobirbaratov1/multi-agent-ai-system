# Runtime dependencies. Upper bounds keep an unattended `pip install` from
# pulling a breaking major release into a deployment.
#
# Test-only dependencies live in requirements-dev.txt.

# Core
anthropic>=0.40.0,<1.0.0
langgraph>=0.2.0,<0.7.0

# API
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.7.0,<3.0.0

# Vector Store
faiss-cpu>=1.8.0,<2.0.0
numpy>=1.26.0,<3.0.0

# Monitoring (optional at runtime — degrades gracefully when unconfigured)
langsmith>=0.1.0,<0.5.0

# Telegram Bot
python-telegram-bot>=21.0.0,<23.0.0

# Utils
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
